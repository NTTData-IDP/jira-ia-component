events {}

http {
    include /etc/nginx/mime.types;

    server {
        listen 8080;

        root /app/dist/jira-ia-component;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}